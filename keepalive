name: Keep Streamlit Alive

on:
  schedule:
    - cron: '*/10 * * * *'  # Every 10 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Keep Alive
        run: curl -s https://yourhotelname.streamlit.app/ > /dev/null
