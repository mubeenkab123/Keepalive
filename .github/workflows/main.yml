name: Keep Streamlit Alive

on:
  schedule:
    - cron: '*/10 * * * *'  # Runs every 10 minutes

jobs:
  keep_alive:
    runs-on: ubuntu-latest
    steps:
      - name: Send Keep-Alive Request
        run: curl -s -o /dev/null https://yourhotelname.streamlit.app/
